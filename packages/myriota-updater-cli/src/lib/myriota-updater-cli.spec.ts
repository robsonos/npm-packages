import { myriotaUpdaterCli } from './myriota-updater-cli';

describe('myriotaUpdaterCli', () => {
  it('should work', () => {
    expect(myriotaUpdaterCli()).toEqual('myriota-updater-cli');
  });
});
